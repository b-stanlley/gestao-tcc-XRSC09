# Backend SINTCC — malha brokerless ZeroMQ + BFF (Python puro / stdlib HTTP).
pyzmq>=26.0
mysql-connector-python>=8.3     # persistencia MySQL (fonte da verdade). Sem o servidor MySQL,
                                # o sistema ainda roda em fallback de memoria automaticamente.
# Opcional:
# requests>=2.31                # provedor de LLM via Ollama (LLM_PROVIDER=ollama).
#                               # Gemini (LLM_PROVIDER=gemini) usa urllib (stdlib) — sem dependencia extra.
